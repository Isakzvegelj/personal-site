# DSH project context

## Workspace

Work on this site inside the isolated Debian VM at `/home/dsh/projects/personal-site`.
The repository was cloned from `https://github.com/Isakzvegelj/personal-site` on the `main` branch; the working tree was clean at clone time.

## Project layout

This is a static personal website with the main page at `index.html`, styles in `css/site.css`, browser code in `js/main.js`, and static assets in `assets/`.
The `blog/` directory contains the blog index, post content, publishing helper, and `GUIDE.md`.
`CNAME`, `robots.txt`, `sitemap.xml`, `.nojekyll`, and `.well-known/security.txt` are deployment metadata.

## Working rules

Keep credentials, private keys, tokens, local browser data, and host-only files out of this workspace.
Review changes before committing or publishing.
Use the VM copy as the working copy; the Mac checkout remains separate.

## Current status

No Mac-only DSH session log or credentials were copied. This file is a safe project-context summary for VM DSH sessions.
