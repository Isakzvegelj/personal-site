# projects/personal-site

## Purpose
Git workspace for a personal-site project. The repository currently has no README at its root, so inspect the tracked files, package manifests, and Git history before assuming the stack or deployment target.

## Agent handoff
Do not infer production behavior from the directory name alone. Preserve existing content and configuration, keep secrets out of the repository, and avoid destructive cleanup of unknown files.

## Release gate
Run the repository's actual checks once identified. Do not commit, push, or deploy without explicit approval. Verify the live target and SEO essentials if a deployment is requested.